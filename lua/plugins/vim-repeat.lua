-- lua/plugins/vim-repeat.lua

return {
	"tpope/vim-repeat", -- Plugin to repeat commands in Neovim
	config = function()
		-- This plugin doesn’t need specific configuration but makes it easier to repeat commands
		-- Just make sure it’s loaded and you can start using it
	end,
}
