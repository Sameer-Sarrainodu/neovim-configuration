-- lua/plugins/vim-surround.lua

return {
	"tpope/vim-surround", -- Plugin for working with surroundings (quotes, parentheses, etc.)
	config = function()
		-- You can configure `vim-surround` here if needed, but it's ready to go by default.
		-- No specific setup needed, as it's simple to use.
	end,
}
